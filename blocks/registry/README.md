# Blocks Registry v1

Canonical runtime lookup files:
- blocks.normalized.tsv
- block_scales.tsv
- divisors_5040.tsv (lane index -> divisor for 60-way lane namespace)

Legacy block source material is archived under:
- ../archive/<timestamp>/legacy/
