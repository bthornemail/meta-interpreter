#+title: dev-docs.org
#+author: Codex
#+startup: overview
#+options: toc:3 num:nil
#+property: header-args :tangle no

* Frontmatter

- Status: interpretive root and compile/export gate
- Authority: non-canonical
- Boundary: may explain runtime law; may not replace it
- Entry role: constitutional root for the Org conversation surface

** Audit State

- audit_scope: embedded corpus | live repo
- audit_time: 2026-04-05
- repo_state: post-cleanup
- symlink_state: removed
- execution_mode: executed
- layout_status: pass
- legacy_path_status: pass (canonical paths only)
- symbolic_reference_status: present
- symbolic_reference_count: 1
- execution_scope: smoke
- entrypoint_coverage: partial (22 items classified: 12 pass, 10 skip, 0 fail, 0 blocked, 0 missing)
- repair_actions_applied: true

This file is the maintained entrypoint for the framework conversation.
It is conversational like an RFC, but it is still rule-bound.

#+begin_quote
The runtime check and processing of files is the only truth.
Everything else is witness, projection, or derivative.
#+end_quote

Address-first reconstruction remains the active baseline:

#+begin_quote
either the address exists or it does not

if it exists:
replay -> class -> point -> lane -> leaf -> address
#+end_quote

** RFC 2119 Usage

The key words =MUST=, =MUST NOT=, =SHOULD=, =SHOULD NOT=, and =MAY= in this
document and the role files are to be interpreted as normative instruction
terms.

** Compile / Export Gate

No section may be treated as compile/export eligible unless it:

- uses RFC 2119 normative language when making enforceable claims
- answers at least one invariant question
- references at least one algorithm lane
- declares authority, surface, and artifact role
- declares proof-burden and convergence metadata

Idea-mode sections MAY remain conversational, but they MUST NOT be treated as
implementation-relevant exports.

** Shared Drawer Vocabulary

Compile-mode sections in the role files SHOULD use these drawers:

- =:mode:= =compile|idea=
- =:artifact_role:= =claim|proposal|closure|receipt=
- =:question:= =Q1..Q8=
- =:algorithm:= =Transition|Control-Plane|Projection|Escape|Fano Path|Proposal/Receipt|Branch/Reconciliation=
- =:authority:= =interpretive-over-runtime=
- =:surface:= free text
- =:maps_to:= file or runtime basis references
- =:constructive_complexity:= =P|NP|PSPACE|EXPTIME|unknown=
- =:falsification_complexity:= =P|NP|PSPACE|EXPTIME|unknown=
- =:convergence_class:= =DTIME|NTIME|PSPACE|EXPTIME|unknown=
- =:convergence_behavior:= =deterministic|branching|bounded-search|exponential|unknown=
- =:status:= =open|closed=
- =:step_identity:= tree-derived semantic identity
- =:derived_from:= prior step in promotion chain
- =:promotes_to:= next step in promotion chain
- =:receipt_for:= closure being receipted

Complexity and convergence metadata describe feasibility and proof burden.
They MUST NOT be treated as semantic ontology.

** Execution Substrate Targets

The implementation MUST declare its execution substrate. Core completion is
measured first by conformance to that substrate.

*** Target Order

| Tier | Name | Description |
|------|------|-------------|
| 0 | substrate_core | BusyBox ash + POSIX sh + awk + core POSIX file/process model |
| 1 | substrate_extended | POSIX system interface subset, Python helpers, browser demos |
| 2 | substrate_speculative | Prolog/WordNet, semantic adapters, higher-order proof tooling |

*** Compliance Model

Use this ranking for test categories:

1. Conformance - MUST pass on declared substrate
2. Functional - SHOULD pass once conformance is met
3. Stress - MAY pass once functional works
4. Performance - MAY pass once stress is stable
5. Speculative - experimental, separate from core

Functional, stress, performance, and speculative behavior are secondary and
MUST NOT redefine the conformance boundary.

*** Short Freeze

#+begin_quote
We are done with the core when the system is conformant to the declared
BusyBox/POSIX substrate. Everything beyond that is extension, optimization,
or speculation.
#+end_quote

** Foundations Lineage

The Haskell files in =research/foundations/= are the typed law-and-scope scaffold
that the current C/gawk framework lowers from.

| Stage | Tool | Purpose |
|-------|------|---------|
| Proof pressure | Coq | Test whether the law can be stated rigorously |
| Scope definition | Haskell | Define real boundaries and executable law |
| Surface language | EDSL/DSL | Shape expression and interfaces |
| Interface boundary | ABI/EABI | Freeze lowering targets |
| Substrate embodiment | C/gawk | Current target (substrate_core) |
| Final kernel | pure bitwise | Intentional lowering when possible |

These files show the explicit ladder:

- =AtomicConstitution.hs= — constitutional alphabet, quotient test, delta law
- =AtomicKernel.hs= — pure DSL with named algorithms, no hidden state
- =Composition.hs= — delta law, replay, phase factorization, header packing
- =ClosureRuntime.hs= — shaped for ABI/EABI lowering, deterministic scheduling
- =Automaton_v2.hs/v3.hs= — canonical stream decoupler, point-set lifting
- =Matroid_v2.hs/v3.hs= — ABI-oriented block design, bounded symbolic families
- =Artifact.hs=, =Tetragrammaton.hs= — law/carrier split, simplex resolution
- =PortMatroid.ts= — same law exported in TypeScript artifact form

*** Post-Freeze Scoping

After the core was frozen, the methodology ladder above is no longer needed for discovery.
The new scoping tests are:

1. Can it be a surface?
2. Can it achieve POSIX compliance?

These two questions replace the full methodology ladder as the boundary for new work.

Typed scaffolds are acceptable when they are used to define scope and can later
be lowered to the same replayable bitwise law.

No scaffold is final unless removing it changes canonical replay.

** Repository Authority Layout

The repository is organized by authority boundary, not by historical growth.

| Directory | Authority | Description |
|-----------|-----------|-------------|
| runtime/ | authoritative | canonical state evolution |
| substrate/ | support | execution support (BusyBox, WordNet) |
| system-image/ | deployment | deployment manifests |
| surfaces/ | downstream | projections (browser, narrative) |
| artifacts/ | outputs | generated outputs |
| docs/ | descriptive | explanatory only |
| archive/ | frozen | legacy |

Within runtime/:

- kernel/ - implementation of runtime law (C/awk sources)
- contracts/ - normative definitions (LEXICON, ONTOLOGY, SURFACES)
- blocks/ - canonical registries

*** Compatibility Links

Symlinks were removed as of 2026-04-05. Direct canonical paths now required:

| Legacy | Canonical |
|--------|-----------|
| src/ | runtime/kernel/ |
| blocks/ | runtime/blocks/ |
| demo/ | surfaces/ |
| deploy/ | system-image/deploy/ |
| packages/ | substrate/ |

** Repair Actions Applied

The following corrections were applied during audit to achieve current state:

- Makefile: SRC_DIR migrated from src to runtime/kernel
- scripts/governance/validate_lexicon.sh: contract path updated from docs/ to runtime/contracts/
- scripts/governance/validate_ontology.sh: contract path updated from docs/ to runtime/contracts/
- scripts/README.md: terminology corrected from "Aztec transport" to "Aztec compat alias"

* 112 Proof Matrix

The framework uses:

- 8 invariant questions
- 7 algorithm lanes
- 2 proof directions for every lane

Therefore:

#+begin_example
8 x 7 x 2 = 112 proofs
#+end_example

This root file defines the matrix and links into the role files.
The role files remain conversational; the matrix gives them grounding.

** Invariant Questions

- Q1: What is the source of truth?
- Q2: How does the system move?
- Q3: How does it distinguish payload from control?
- Q4: How does it preserve meaning across projections?
- Q5: How does it allow intervention without losing replay?
- Q6: How does it permit collaboration without hidden authority?
- Q7: How does it branch and return without fragmentation?
- Q8: How does it stay open in meaning while closed in law?

** Algorithm Lanes

- Transition
- Control-Plane
- Projection
- Escape
- Fano Path
- Proposal/Receipt
- Branch/Reconciliation

** Role Files

- [[file:org/claims.org][org/claims.org]]
- [[file:org/proposals.org][org/proposals.org]]
- [[file:org/closure.org][org/closure.org]]
- [[file:org/receipts.org][org/receipts.org]]

** Matrix Index

Legend:

- =impl= means direct executable evidence exists in the current repo
- =doc= means the section is grounded by runtime, docs, or validators but not by a dedicated proof harness
- =miss= means no grounded section exists yet

| Question | Transition | Control-Plane | Projection | Escape | Fano Path | Proposal/Receipt | Branch/Reconciliation |
|----------+------------+---------------+------------+--------+-----------+------------------+-----------------------|
| Q1 | [[file:org/claims.org::#q1-transition][impl]] | [[file:org/claims.org::#q1-control-plane][doc]] | [[file:org/receipts.org::#q1-projection][doc]] | [[file:org/proposals.org::#q1-escape][doc]] | [[file:org/proposals.org::#q1-fano-path][doc]] | [[file:org/proposals.org::#q1-proposal-receipt][doc]] | [[file:org/closure.org::#q1-branch-reconciliation][doc]] |
| Q2 | [[file:org/claims.org::#q2-transition][impl]] | [[file:org/claims.org::#q2-control-plane][doc]] | [[file:org/receipts.org::#q2-projection][doc]] | [[file:org/proposals.org::#q2-escape][doc]] | [[file:org/proposals.org::#q2-fano-path][doc]] | [[file:org/proposals.org::#q2-proposal-receipt][doc]] | [[file:org/closure.org::#q2-branch-reconciliation][doc]] |
| Q3 | [[file:org/claims.org::#q3-transition][doc]] | [[file:org/claims.org::#q3-control-plane][doc]] | [[file:org/receipts.org::#q3-projection][doc]] | [[file:org/proposals.org::#q3-escape][doc]] | [[file:org/proposals.org::#q3-fano-path][doc]] | [[file:org/proposals.org::#q3-proposal-receipt][doc]] | [[file:org/closure.org::#q3-branch-reconciliation][doc]] |
| Q4 | [[file:org/claims.org::#q4-transition][doc]] | [[file:org/claims.org::#q4-control-plane][doc]] | [[file:org/receipts.org::#q4-projection][impl]] | [[file:org/proposals.org::#q4-escape][doc]] | [[file:org/proposals.org::#q4-fano-path][doc]] | [[file:org/proposals.org::#q4-proposal-receipt][doc]] | [[file:org/closure.org::#q4-branch-reconciliation][doc]] |
| Q5 | [[file:org/claims.org::#q5-transition][doc]] | [[file:org/claims.org::#q5-control-plane][doc]] | [[file:org/receipts.org::#q5-projection][doc]] | [[file:org/proposals.org::#q5-escape][doc]] | [[file:org/proposals.org::#q5-fano-path][doc]] | [[file:org/proposals.org::#q5-proposal-receipt][doc]] | [[file:org/closure.org::#q5-branch-reconciliation][doc]] |
| Q6 | [[file:org/claims.org::#q6-transition][doc]] | [[file:org/claims.org::#q6-control-plane][doc]] | [[file:org/receipts.org::#q6-projection][doc]] | [[file:org/proposals.org::#q6-escape][doc]] | [[file:org/proposals.org::#q6-fano-path][doc]] | [[file:org/proposals.org::#q6-proposal-receipt][impl]] | [[file:org/closure.org::#q6-branch-reconciliation][doc]] |
| Q7 | [[file:org/claims.org::#q7-transition][doc]] | [[file:org/claims.org::#q7-control-plane][doc]] | [[file:org/receipts.org::#q7-projection][doc]] | [[file:org/proposals.org::#q7-escape][doc]] | [[file:org/proposals.org::#q7-fano-path][doc]] | [[file:org/proposals.org::#q7-proposal-receipt][doc]] | [[file:org/closure.org::#q7-branch-reconciliation][doc]] |
| Q8 | [[file:org/claims.org::#q8-transition][doc]] | [[file:org/claims.org::#q8-control-plane][doc]] | [[file:org/receipts.org::#q8-projection][doc]] | [[file:org/proposals.org::#q8-escape][doc]] | [[file:org/proposals.org::#q8-fano-path][doc]] | [[file:org/proposals.org::#q8-proposal-receipt][doc]] | [[file:org/closure.org::#q8-branch-reconciliation][doc]] |

* Current Executable Coverage

The current repo does not contain a dedicated =tests/= harness for the 112
matrix. Direct executable evidence therefore comes from existing runtime
checks, validators, and make targets.

- Q1 / Transition = implemented
  - source: =make framework-check=
  - basis: =scripts/validate_framework.sh=
  - evidence: deterministic replay receipt, identical runtime layer emission, slot roundtrip
- Q2 / Transition = implemented
  - source: =make framework-check=
  - basis: =scripts/validate_framework.sh=
  - evidence: same input yields same next-state emission; altered seed changes only rule-selected output
- Q4 / Projection = implemented
  - source: =make projection-check= and =make media-check=
  - basis: =scripts/projection/validate_projection_render.py= and =scripts/projection/validate_media_render.py=
  - evidence: multiple projection surfaces preserve the same selected identity and witness hashes
- Q6 / Proposal/Receipt = implemented
  - source: =make narrative-check=
  - basis: =scripts/narrative/validate_narrative_binding.py=
  - evidence: deferred narrative/projection artifacts remain reproducible and identity-stable

Indirect but grounded evidence exists for other sections through:

- =make governance-audit-check=
- =make narrative-frame-check=
- runtime exports under =bin/ttc_framework=
- normative docs under =docs/=

Sections marked =doc= are grounded by those sources but do not yet have a
dedicated proof-harness cell.

* Semantic Adapter Boundary

Semantic adapters are downstream query, relation, and expression surfaces.
They are not runtime law.

This includes:

- S-expressions
- M-expressions
- Datalog
- Prolog
- recursive fixed-point discussion
- WordNet-style semantic lattice overlays
- the currently present substrate tree under [[file:substrate/prologdb-5WN][substrate/prologdb-5WN]]

Normative rules:

- Semantic adapters MAY query and relate canonical exports.
- Semantic adapters MUST NOT define canonical truth.
- Semantic adapters MUST NOT define address existence.
- Semantic adapters MUST NOT define closure.
- WordNet-style overlays MAY support naming, subsumption, and neighborhood queries.
- WordNet-style overlays MUST NOT replace replay-derived structure.

* Appendix Links

- [[*Speculative Appendix][Speculative Appendix]]

* Speculative Appendix

** 8-Tuple Correspondence
:PROPERTIES:
:mode: idea
:authority: non-canonical
:status: speculative
:surface: research appendix
:END:

The 2DFA / R5RS / octonion correspondence is retained only as a speculative
research scaffold.

It MAY support future discussion about response dialects or organizational
lenses.
It MUST NOT be treated as runtime law, address law, closure law, or proof of
formal isomorphism without smaller proof obligations.
