Typed deterministic replay is sovereign; narrative binding is an advisory witness overlay.

# Narrative Witness Binding

## Purpose

Bind canonical narrative chapter NDJSON to:
- structural progression templates
- witness-plane advisory hooks
- downstream projection hints

without changing canonical chapter records or runtime law.

## Authority Boundary

- `demo/narrative_data/chapters/*.ndjson` remain the canonical narrative corpus in this repository.
- `demo/narrative_data/templates/character_progression_templates.json` provides canonical reusable graph shapes.
- `demo/narrative_data/witness_article_hooks.json` provides advisory witness hook bindings.
- `demo/narrative_data/narrative.bound.v0.ndjson` is a derived witness artifact only.

Narrative binding MUST NOT:
- mutate canonical chapter records
- redefine runtime, incidence, grammar, or scheduling law
- promote witness interpretation to canonical authority

## Data Flow

```text
canonical chapter NDJSON
-> deterministic binder
-> narrative witness binding artifact
-> narrative witness UI / projection
```

## Binding Artifact

The binder emits:

1. `narrative_bound_chapter`
- chapter id, title, phase
- selected template id
- witness role
- matched witness hook ids

2. `narrative_bound_step`
- canonical chapter/scene/semantic transition identity
- deterministic template transition binding
- witness role + advisory checks + receipt templates
- projection hints for text/audio/visual lanes

## Witness Roles

Roles are selected from the canonical witness-plane role set:
- `advisor.wisdom`
- `advisor.law`
- `advisor.cohesion`
- `witness.observe`

The binder may classify a chapter into one of these roles.
That role remains advisory. It does not grant mutation authority.

## UI Boundary

The narrative witness page is a projection consumer only.
Its primary controls follow the strict attention grammar:

- `Mode`
- `Frame`
- `Attention`
- `Depth`

Secondary chapter and step tools are hidden behind `More`.

The page builds one normalized narrative scene object per selected bound step.
That scene object is the single downstream source for:
- SVG witness rendering now
- future canvas animation
- future A-Frame rendering

Future 3D rendering must consume the same normalized scene object used by SVG:
- nodes map to 3D anchors
- edges map to 3D lines or tubes
- active transition maps to elevation/highlight only
- no 3D-specific semantics may be introduced
- canonical NDJSON must not be read directly from 3D renderers

Local frame interpolation is reserved for later projection work only.
If added, it must interpolate between normalized scene objects and must not
alter chapter identity, step identity, or canonical reconstruction.

## Reproducibility

The binding artifacts are generated by:
- [bind_narrative_to_witness.py](/home/main/Programs/meta-interpreter/scripts/bind_narrative_to_witness.py)

Frozen invariant:

> Same canonical chapter corpus + same templates + same witness hooks must always yield the same narrative witness artifact; any witness/media/UI adaptation may change only downstream presentation, never canonical narrative authority.

Reproducibility is enforced by:
- `make narrative-check`

That check proves only:
- the binding artifacts are deterministic and reproducible
- the witness page preserves the required attention-law control labels
- the same bound step yields the same normalized scene object and the same SVG witness
- attention/depth changes affect presentation only, not selected chapter/step identity

It does not elevate the narrative witness page to runtime authority.
